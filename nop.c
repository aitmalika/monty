#include "monty.h"

/**
 * nop - Lol the does nothing
 * @stack: doesn't mater
 * @line_cnt : for the nothing
 *
 * Return: nothing
 */
void nop(stack_t **stack, unsigned int line_cnt)
{
	(void) stack;
	(void) line_cnt;
}
